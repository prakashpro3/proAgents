# Phase 0: Initialization Wizard

Interactive project setup with project type selection.

---

## Trigger

User runs `pa:init` or starts a new project conversation.

---

## Initialization Flow

### Step 1: Project Type Selection

**Prompt to User:**
```
Welcome to ProAgents! Let's set up your project.

What type of project are you developing?

1. Web Frontend      - SPA, PWA, static sites (React, Vue, Angular)
2. Full-stack        - Frontend + Backend (Next.js, Nuxt, Remix)
3. Mobile            - iOS, Android, cross-platform (React Native, Flutter)
4. Desktop           - Windows, macOS, Linux apps (Electron, Tauri)
5. Backend/API       - REST, GraphQL, microservices (Node, Python, Go)
6. CLI Tool          - Command-line applications
7. Library/Package   - Reusable packages (npm, PyPI)
8. Microservices     - Distributed systems
9. Monorepo          - Multiple related projects

Select (1-9) or describe your project:
```

---

### Step 2: Framework Selection (Based on Type)

**For Web Frontend:**
```
Which framework are you using?

1. React (Vite) - Recommended
2. React (Create React App)
3. Vue 3 (Vite)
4. Angular
5. Svelte
6. Solid.js
7. Vanilla JavaScript
8. Other (specify)

Select or describe:
```

**For Full-stack:**
```
Which full-stack framework?

1. Next.js (React) - Recommended
2. Nuxt 3 (Vue)
3. SvelteKit
4. Remix
5. Astro (with SSR)
6. T3 Stack (Next.js + tRPC)
7. Other (specify)

Select or describe:
```

**For Mobile:**
```
Which mobile framework?

1. React Native - Recommended for React devs
2. Flutter - Recommended for new projects
3. Native iOS (Swift)
4. Native Android (Kotlin)
5. Ionic/Capacitor
6. Expo (React Native managed)
7. Other (specify)

Select or describe:
```

**For Desktop:**
```
Which desktop framework?

1. Electron - Recommended
2. Tauri (Rust + Web)
3. Qt (C++/Python)
4. .NET MAUI
5. Flutter Desktop
6. Other (specify)

Select or describe:
```

**For Backend/API:**
```
Which backend framework?

1. Node.js + Express
2. Node.js + Fastify
3. Node.js + NestJS - Recommended
4. Python + FastAPI - Recommended
5. Python + Django
6. Go + Gin
7. Rust + Axum
8. Other (specify)

Select or describe:
```

---

### Step 3: Language & Tools

**For JavaScript/TypeScript projects:**
```
Language and tools:

1. TypeScript - Recommended
2. JavaScript

Package manager:
1. npm
2. pnpm - Recommended
3. yarn
4. bun
```

**Common tools selection:**
```
Select tools to include (multi-select):

[ ] Testing Framework (Jest/Vitest)
[ ] Linting (ESLint)
[ ] Formatting (Prettier)
[ ] Git Hooks (Husky)
[ ] CI/CD (GitHub Actions)
[ ] Docker
[ ] Database (if applicable)
```

---

### Step 4: Project Details

```
Project name: _______________
Description: _______________

Is this:
1. A new project (I'll create the structure)
2. An existing project (analyze current codebase)
```

---

### Step 5: Coding Standards

```
Which coding standards should we follow?

1. ProAgents Default - Balanced, pragmatic rules
2. Airbnb Style Guide - Strict, comprehensive
3. Google Style Guide - Clean, minimal
4. Standard JS - No config needed
5. Custom - I'll define my own
6. Existing - Use my project's existing rules

Select:
```

---

### Step 6: Workflow Checkpoints

```
Configure workflow checkpoints (pause for approval):

[ ] After codebase analysis
[x] After design/planning - Recommended
[ ] After implementation
[x] Before deployment - Recommended
[ ] After each phase

Select checkpoints or use defaults:
```

---

## Output: Generated Configuration

After wizard completion, generate `proagents.config.yaml`:

```yaml
# Generated by ProAgents Init Wizard
# Date: 2024-01-15

project:
  name: "my-awesome-app"
  description: "A web application for..."
  type: "web-frontend"
  framework: "react-vite"
  language: "typescript"
  package_manager: "pnpm"

# Phase configuration based on project type
phases:
  analysis:
    enabled: true
    depth: "full"
  requirements:
    enabled: true
  ui_design:
    enabled: true
    guidelines: ["react-patterns"]
  api_design:
    enabled: false  # Skipped for frontend-only
  planning:
    enabled: true
  implementation:
    enabled: true
  testing:
    enabled: true
    coverage_target: 80
  code_review:
    enabled: true
  documentation:
    enabled: true
    mode: "moderate"
  deployment:
    enabled: true
    target: "static-hosting"

# Workflow checkpoints
checkpoints:
  after_analysis: false
  after_requirements: false
  after_design: true
  after_implementation: false
  after_testing: false
  before_deployment: true

# Git configuration
git:
  enabled: true
  branch_prefix: "feature/"
  commit_convention: "conventional"
  require_pr: true

# Tools
tools:
  testing:
    framework: "vitest"
    coverage: true
  linting:
    eslint: true
    config: "recommended"
  formatting:
    prettier: true
  git_hooks:
    husky: true
    commitlint: true

# Standards
standards:
  style: "proagents-default"
  architecture: "feature-based"
```

---

## Type-Specific Setup

### Web Frontend Setup
```
Creating Web Frontend project...

Structure:
├── src/
│   ├── components/
│   ├── pages/
│   ├── hooks/
│   ├── services/
│   ├── stores/
│   ├── utils/
│   └── types/
├── public/
├── tests/
└── [config files]

Tools configured:
✓ Vite (build tool)
✓ React 18
✓ TypeScript
✓ React Router
✓ Vitest + Testing Library
✓ ESLint + Prettier

ProAgents configured for web frontend workflow.
```

### Mobile Setup
```
Creating Mobile (React Native) project...

Structure:
├── src/
│   ├── screens/
│   ├── components/
│   ├── navigation/
│   ├── services/
│   ├── stores/
│   └── utils/
├── __tests__/
├── ios/
├── android/
└── [config files]

Tools configured:
✓ React Native 0.73
✓ TypeScript
✓ React Navigation
✓ Jest + Testing Library
✓ ESLint

ProAgents configured for mobile workflow.
Platforms: iOS, Android
```

### Backend/API Setup
```
Creating Backend API project...

Structure:
├── src/
│   ├── controllers/
│   ├── services/
│   ├── models/
│   ├── middleware/
│   ├── routes/
│   └── utils/
├── tests/
├── docs/
└── [config files]

Tools configured:
✓ Node.js + Express
✓ TypeScript
✓ Prisma (ORM)
✓ Jest + Supertest
✓ ESLint
✓ OpenAPI/Swagger

ProAgents configured for backend workflow.
```

---

## Quick Init Options

For experienced users who want to skip the wizard:

```bash
# Full wizard (default)
/init

# Quick init with type
pa:init --type web-frontend
pa:init --type mobile --framework react-native
pa:init --type backend --framework nestjs

# Use existing config
pa:init --config ./proagents.config.yaml

# Minimal setup (prompts only essential questions)
pa:init --quick

# Skip all prompts, use all defaults
pa:init --yes
```

---

## Post-Init Actions

After initialization:

1. **Display next steps:**
```
ProAgents initialized successfully!

Next steps:
1. Review proagents.config.yaml
2. Start your first feature: pa:feature "Feature name"
3. Need help? Run pa:help

Your project type: Web Frontend (React)
Recommended workflow:
  pa:feature → pa:analyze → Design → pa:implement → pa:test → pa:deploy
```

2. **Create initial files:**
   - `proagents.config.yaml`
   - `.proagents/` directory
   - `CODEOWNERS` (if team project)

3. **Detect existing project:**
   If files already exist, analyze and adapt configuration.

---

## Slash Commands

| Command | Description |
|---------|-------------|
| `pa:init` | Full initialization wizard |
| `pa:init --type [type]` | Quick init with project type |
| `pa:init --wizard` | Force full wizard |
| `pa:init --quick` | Minimal setup |
| `pa:init --config [file]` | Use existing config |
| `pa:setup` | Re-run setup for existing project |
